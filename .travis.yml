language: ruby
# If you don't specify a version, Travis CI will use MRI 1.9.3 as the default.
# http://docs.travis-ci.com/user/languages/ruby/
rvm:
  - 2.2.0
services:
- mongodb
- elasticsearch
env:
  # This needs to be set here because TravisCI doesn't
  # let untrusted sources run on mailchimp. Anyway, this is
  # fake.
  - MAILCHIMP_API_KEY=mailchimp-abc
